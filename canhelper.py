#!/usr/bin/env python3
#

IP_OFFSET = 100

CAN_IDS = {
    # 101 + IP_OFFSET: 0x463,
    # 102 + IP_OFFSET: 0x461,
    # 103 + IP_OFFSET: 0x453,
    # 104 + IP_OFFSET: 0x451,
    # 105 + IP_OFFSET: 0x443,
    # 106 + IP_OFFSET: 0x441,
    # 107 + IP_OFFSET: 0x433,
    # 108 + IP_OFFSET: 0x431,
    # 109 + IP_OFFSET: 0x423,
    # 110 + IP_OFFSET: 0x421,

    101 + IP_OFFSET: 0x421,
}

